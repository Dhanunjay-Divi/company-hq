# Source packaging record

Prepared 2026-09-14 UTC for the user's private `Dhanunjay-Divi/company-hq` repository. Source came from the existing local shared toolkit; the running installation was not moved or replaced.

An explicit allowlist was used. Preserved the current frontend and vendored graph/assets/licenses, native bridge and schemas, ClawTeam wrappers/legacy fallback, Ruflo adapter/lock, code graph guard/checksums, optional Graft manifests/wrapper, earlier Swift board, discovery/kickoff/update code and tests, four role templates, operating skill, guides and historical seven-file Agent Teams compatibility patch including untracked tests.

The existing app SOURCE.zip was only the initial seed. Missing toolkit files, command wrappers, earlier work and dependency provenance were added explicitly. The source archive itself is generated output and is not committed. Unmodified upstream repositories are represented by pinned provenance/dependencies rather than their entire downloaded histories/caches.

Excluded account credentials/configuration, Codex private task history, model transcripts/receipts, per-project tasks/messages/memories, indexes/databases, local project inventory, product integrity dumps, binaries/apps, backups, dependency caches and virtual environments. Historical source documentation still contains original-machine paths and references to earlier test fixtures; these are not a portable installation recipe. See STATUS.md.

The implementation is staged in a separate checkout at `/Users/uno/Projects/company-hq`. Product files and the live toolkit's state are not deployment targets of this packaging operation. Generated npm files stay ignored. Candidate GitHub metadata was fetched read-only; no new candidate installer, hook, subscription or framework runtime was activated.

`python3 scripts/check_source_bundle.py` checks tracked filenames and common credential signatures without printing secret values. This is a conservative packaging check, not a comprehensive security audit. Review the staged file list before publishing.

## Validation for this repository

Final actual commands/results are recorded in REPOSITORY-VERIFICATION.md. Earlier integration verification is preserved as historical evidence; clean-clone and platform gaps remain explicit. Repository visibility is checked after GitHub creation and upload.
